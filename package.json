{
  "name": "hardened-http-server",
  "version": "1.0.0",
  "description": "Production-grade, dependency-free Node.js HTTP server with error handling, graceful shutdown, input validation, resource cleanup, and robust request processing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}
